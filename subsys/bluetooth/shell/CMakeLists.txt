zephyr_library()
zephyr_library_sources(
  bt.c
  hci.c
  )
zephyr_library_sources_ifdef(
  CONFIG_BT_CONN
  gatt.c
  )
zephyr_library_sources_ifdef(
  CONFIG_BT_CTLR
  ll.c
  ticker.c
  )
zephyr_library_sources_ifdef(
  CONFIG_SOC_FLASH_NRF
  flash.c
  )
if(CONFIG_SOC_FAMILY_NRF)
  zephyr_library_include_directories(
    ../controller/ll_sw/nordic
  )
endif()
